/** @type {import('next').NextConfig} */
// const nextConfig = {}

module.exports = {
  images: {
    domains: ["murotov.netlify.app"],
  },
  output: {
    logging: {
      browser: true,
      console: true,
    },
  },
};

// module.exports = nextConfig
